# Docker Environment Configuration for Baloo
# Copy this to .env and fill in your actual values

# GitHub App Configuration
GITHUB_APP_ID=your_app_id_here
# Recommended: keep the private key file in .secrets/ and point to it here.
GITHUB_PRIVATE_KEY=.secrets/your-github-app.private-key.pem
# You can also inline the PEM contents if you prefer.
# GITHUB_PRIVATE_KEY=-----BEGIN RSA PRIVATE KEY-----\nYour private key here...\n-----END RSA PRIVATE KEY-----
GITHUB_WEBHOOK_SECRET=your_webhook_secret_here

# LLM Configuration
ANTHROPIC_API_KEY=sk-ant-your_api_key_here
GEMINI_API_KEY=your_gemini_api_key_here

# Application Configuration
APP_ENVIRONMENT=production
APP_HOST=0.0.0.0
APP_PORT=8000
LOG_LEVEL=INFO
MAX_CONCURRENT_REVIEWS=3
WEBHOOK_DELIVERY_DEDUPE_TTL_SECONDS=900

# Agent Configuration (short names: flash, haiku, sonnet, gemini-pro, standard, premium, opus)
AGENT_PROVIDER=google
AGENT_MODEL=premium
AGENT_FALLBACK_MODEL=anthropic/claude-sonnet-4-6
AGENT_MAX_TOKENS=4096
AGENT_TEMPERATURE=0.2
PI_BINARY_PATH=pi

# Review Configuration
TICKET_ID_PREFIX=PROJ
ALLOWED_REPOSITORIES=
REVIEW_AUTO_APPROVE=false
REVIEW_MIN_SEVERITY=MEDIUM
REVIEW_USE_CHECKS_API=true

# Database Configuration
DATABASE_ENABLED=false
# The default docker-compose stack injects the local db connection automatically.
DATABASE_URL=

# Dashboard Configuration
DASHBOARD_ENABLED=false
DASHBOARD_USERNAME=
DASHBOARD_PASSWORD=

# Fidelity Configuration
FIDELITY_ENABLED=true
FIDELITY_PLAN_PATH_PATTERN=docs/plans/{ticket_id}.md
FIDELITY_APPROVAL_THRESHOLD=90
